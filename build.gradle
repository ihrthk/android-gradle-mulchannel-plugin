apply plugin: 'maven'
apply plugin: 'groovy'

buildscript {
    repositories {
        mavenLocal()
        jcenter()
    }
}

group = 'me.zhangls'
archivesBaseName = 'mulchannel'
version = '0.0.1'


dependencies {
    compile gradleApi()
    compile localGroovy()
}