apply plugin: 'maven'
apply plugin: 'groovy'

buildscript {
    repositories {
        mavenLocal()
        jcenter()
    }
}

group = 'org.greeting'
archivesBaseName = 'greeting'
version = '0.0.2'


dependencies {
    compile gradleApi()
    compile localGroovy()
}